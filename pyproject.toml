[project]
name = "ng-video-lecture"
version = "0.1.0"
description = "Code from Karpathy's Neural Networks: Zero to Hero nanoGPT lecture."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.5",
]
